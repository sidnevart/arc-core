This directory exists only as a Wails CLI shim.

The canonical native desktop frontend assets live in:

- `apps/desktop/wailsapp/frontend`

Wails still expects frontend install/build hooks relative to the launcher project,
so `package.json` here is intentionally minimal.
